import json
from groq import AsyncGroq, APIStatusError, APIConnectionError, APITimeoutError
from dotenv import load_dotenv
import logging
from config import settings
from core.groq_errors import GroqRateLimitExhausted, is_rate_limit_error, extract_retry_after

load_dotenv()
logger = logging.getLogger(__name__)# если конфиг логгера ещё нигде в проекте не настраивал (basicConfig),
                                    # добавь один раз в точке входа (main.py),
                                    # иначе warning уйдёт в никуда без хендлера:
                                    # logging.basicConfig(level=logging.INFO)


# groq_client = AsyncGroq(api_key=os.getenv("GROQ_API_KEY")) #v1
groq_client = AsyncGroq(api_key=settings.groq_api_key) #v2-сделали config.py


INTENT_CLASSIFICATION_PROMPT = """Ты классифицируешь входящие сообщения в саппорт-чат компании, которая занимается импортом оборудования, автомобилей и запчастей из-за границы в Россию.

Твоя задача — определить главное намерение пользователя.

Верни строго JSON:
{"intent": "категория"}

Не добавляй пояснений, markdown или дополнительного текста.

Доступные категории:

1. вопрос_по_продукту

Вопросы ДО покупки или вопросы о характеристиках товара:
- характеристики оборудования или автомобиля;
- модели, комплектации;
- совместимость;
- наличие товара;
- технические параметры;
- документы на товар;
- гарантия как условие покупки;
- стоимость товара (если нет вопроса про оплату);
- наличие каталога.

Примеры:
"Какая мощность у станка?"
"Этот двигатель подойдет на Toyota Land Cruiser 2018?"
"Какая комплектация автомобиля?"
"Есть ли документы на оборудование?"

---

2. вопрос_по_компании

Общие вопросы о компании, не связанные напрямую с конкретным товаром:
- кто вы;
- где находитесь;
- сайт;
- контакты;
- режим работы;
- юридическая информация;
- производитель или посредник

Примеры:
"Где ваш офис?"
"Какой у вас юридический адрес?"
"Есть сайт посмотреть каталог?"
"Вы сами импортируете или работаете через посредников?"

---

3. оплата

Все вопросы, связанные с деньгами и платежами:
- проблемы с платежами;
- курс;
- счета;
- валюта;
- комиссии;
- рассрочка;
- способы оплаты;
- реквизиты;

Примеры:
"Деньги списались два раза"
"Можно оплатить в юанях?"
"Пришлите реквизиты"
"Есть рассрочка?"

---

4. техподдержка

Проблемы после получения товара или вопросы по эксплуатации:
- оборудование не работает;
- ошибка;
- неисправность;
- настройка;
- подключение;
- обслуживание;
- инструкция по эксплуатации.

Примеры:
"Станок не включается"
"Ошибка E04 на панели"
"Как откалибровать оборудование?"
"Как подключить к 380В?"

---

5. доставка

Все вопросы о перемещении товара:
- где груз;
- статус заказа;
- сроки доставки;
- таможня;
- транспортировка;
- самовывоз;
- логистика.

Примеры:
"Где мой груз?"
"Когда будет доставка?"
"На какой стадии растаможка?"
"Можно забрать самовывозом?"

---

6. жалоба

Сообщение выражает недовольство компанией, качеством сервиса или требует решения конфликта.

Используй эту категорию только если главная цель сообщения — выразить претензию.

Примеры:
"Вы меня обманули"
"Очень плохой сервис"
"Требую вернуть деньги"
"Никто не отвечает уже неделю"

ВАЖНО:
Если пользователь жалуется, но основной вопрос относится к другой категории — выбирай основную категорию.

Примеры:
"Где мой груз? Надоело ждать!" → доставка
"Станок сломался, помогите!" → техподдержка

---

7. спам

Сообщения, которые не относятся к работе компании:
- реклама;
- продвижение услуг;
- казино;
- крипта;
- заработок;
- мошеннические предложения;
- массовые рассылки.

Примеры:
"Заработок 100000 в день"
"Купите подписчиков"
"Крипта без вложений"

---

8. другое

Короткие нейтральные сообщения без содержательного запроса:
- приветствия;
- благодарности;
- подтверждения;
- простые реплики.

Примеры:
"Здравствуйте"
"Спасибо"
"Ок"
"Понял"
"Ага"

---

Правила выбора при конфликте категорий:

1. Если есть проблема с работой оборудования → техподдержка.
2. Если вопрос про статус груза/заказа → доставка.
3. Если вопрос про деньги → оплата.
4. Если пользователь просто недоволен без конкретного вопроса → жалоба.
5. Если вопрос относится к покупке товара → вопрос_по_продукту.
6. Если вопрос относится к самой компании → вопрос_по_компании.
7. Если сообщение не связано с бизнесом → спам.
8. Если нет смысла классифицировать → другое.

Верни JSON строго такого формата: {"intent": "категория"}, без markdown, без пояснений."""

VALID_INTENTS = {
    "вопрос_по_продукту", 'вопрос_по_компании', "оплата", "жалоба",
    "техподдержка", "доставка", "спам", "другое"
}

async def classify_intent(message_text: str) -> str:
    try:
        response = await groq_client.chat.completions.create(
            model="llama-3.3-70b-versatile",
            messages=[
                {"role": "system", "content": INTENT_CLASSIFICATION_PROMPT},
                {"role": "user", "content": message_text}
            ],
            response_format={"type": "json_object"},
            temperature=0.1,
            max_tokens=100,
        )
        raw = response.choices[0].message.content
        parsed = json.loads(raw)
        intent = parsed.get("intent", "").strip()

        if intent not in VALID_INTENTS:
            return "другое"
        return intent

    except (json.JSONDecodeError, KeyError):
        logger.warning("Groq вернул кривой JSON, переключаюсь на fallback-модель")
        return await classify_intent_fallback(message_text)

    except (APIStatusError, APIConnectionError, APITimeoutError) as e:
        logger.warning(f"Основная модель недоступна ({e}), переключаюсь на fallback-модель")
        return await classify_intent_fallback(message_text)

    except Exception as e:
        logger.warning(f"Groq classify failed непредвиденно: {e}")
        return "другое"


async def classify_intent_fallback(message_text: str) -> str:
    try:
        response = await groq_client.chat.completions.create(
            model="llama-3.1-8b-instant",
            messages=[
                {"role": "system", "content": INTENT_CLASSIFICATION_PROMPT},
                {"role": "user", "content": message_text}
            ],
            response_format={"type": "json_object"},
            temperature=0.1,
            max_tokens=100,
        )
        parsed = json.loads(response.choices[0].message.content)
        intent = parsed.get("intent", "").strip()
        return intent if intent in VALID_INTENTS else "другое"
    except Exception as e:
        logger.warning(f"Fallback-модель тоже недоступна ({e}), отдаю 'другое'")
        return "другое"